DJANGO_DEBUG=true
# Required in production — must map to Django SECRET_KEY (empty → broken sessions / admin 500)
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,172.237.96.41
# Optional: comma-separated, e.g. https://yourdomain.com — if unset, http://<each ALLOWED_HOST> is used
# DJANGO_CSRF_TRUSTED_ORIGINS=http://172.237.96.41
# Optional CORS for SPA (comma-separated)
# DJANGO_CORS_ORIGINS=http://172.237.96.41,http://localhost:5173

# PostgreSQL (default). Create DB first, e.g.:
#   createuser -P portfolio
#   createdb -O portfolio portfolio
POSTGRES_DB=portfolio
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Use SQLite instead: DATABASE_ENGINE=sqlite
